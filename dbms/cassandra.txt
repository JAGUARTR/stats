1. Use Keyspace

USE bigdata;

2. Create Table


CREATE TABLE users (
    userid INT PRIMARY KEY,
    name TEXT,
    age INT,
    email TEXT,
    city TEXT
);

3. Insert Data (CRUD: Create)

INSERT INTO users (userid, name, age, email, city)
VALUES (1, 'Alice', 25, 'alice@gmail.com', 'Chennai');

INSERT INTO users (userid, name, age, email, city)
VALUES (2, 'Bob', 30, 'bob@gmail.com', 'Mumbai');

INSERT INTO users (userid, name, age, email, city)
VALUES (3, 'Carol', 28, 'carol@gmail.com', 'Bangalore');

INSERT INTO users (userid, name, age, email, city)
VALUES (4, 'David', 35, 'david@gmail.com', 'Delhi');

INSERT INTO users (userid, name, age, email, city)
VALUES (5, 'Eva', 27, 'eva@gmail.com', 'Hyderabad');

Of course. Here is the complete CQL code, following your format, with the userid changed from UUID to INT.

All the UUID values have been replaced with simple integers (1, 2, 3, 4, 5), and the UPDATE and DELETE commands have been adjusted to use these new integer IDs.

CQL Console
1. Use Keyspace
Code snippet

USE bigdata;
2. Create Table
-- The userid column is now INT instead of UUID.

Code snippet

CREATE TABLE users (
    userid INT PRIMARY KEY,
    name TEXT,
    age INT,
    email TEXT,
    city TEXT
);
3. Insert Data (CRUD: Create)
-- Inserting records with simple integer IDs.

Code snippet

INSERT INTO users (userid, name, age, email, city)
VALUES (1, 'Alice', 25, 'alice@gmail.com', 'Chennai');

INSERT INTO users (userid, name, age, email, city)
VALUES (2, 'Bob', 30, 'bob@gmail.com', 'Mumbai');

INSERT INTO users (userid, name, age, email, city)
VALUES (3, 'Carol', 28, 'carol@gmail.com', 'Bangalore');

INSERT INTO users (userid, name, age, email, city)
VALUES (4, 'David', 35, 'david@gmail.com', 'Delhi');

INSERT INTO users (userid, name, age, email, city)
VALUES (5, 'Eva', 27, 'eva@gmail.com', 'Hyderabad');

4. Display Records (CRUD: Read)

SELECT * FROM users;

5. Update Record (CRUD: Update)


UPDATE users
SET age = 31,
city = 'Pune'
WHERE userid = 5;

6. Delete Record (CRUD: Delete)

DELETE FROM users
WHERE userid = 3;


#confirm deletion

SELECT * FROM users;

7. Drop Table

DROP TABLE users;